doctype html
html(lang="ru") 
    head
        meta(charset="utf-8")
        title Fifteen
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet" href="/css/reset.css")
        link(rel="stylesheet" href="/css/index.css")
    body
        - var values = new Array(16).fill(0).map((_, index) => index + 1)
        .page
            h1 Игра в пятнашки
            .fifteen#fifteen
                each val in values 
                    button.item(data-matrix-id=val)
                        span.itemVal= val 
            button.button#shuffle Перемешать
            
        .modal#fifteen
                .modal-content
                    span.close 
                    h2 Поздравляем!
                    p Вы выиграли в игре "Пятнашки"!
                    button.ok-button Закрыть


        script(src="/js/index.js")
